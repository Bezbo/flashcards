<h3>Колода</h3>

<table class="table-condensed">
  <tr>
    <td>Имя колоды: <%= @deck.name %></td>
  </tr>
  <tr>
    <td>Количество карточек в колоде: <%= @deck.cards.count %></td>
  </tr>
  <tr>
    <td><%= link_to "Редактировать", edit_deck_path(@deck),
                                     class: "btn btn-default" %></td>
  </tr>
  <tr>
    <td><%= link_to "Удалить", @deck, method: :delete,
                                      data: { confirm: "Удалить?" },
                                      class: "btn btn-default" %></td>
  </tr>
</table>

<h4>В это колоде находятся следующие карточки:</h4>

<table class="table">
  <thead>
    <tr>
      <th>Оригинал</th>
      <th>Перевод</th>
      <th>Дата пересмотра</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @deck.cards.each do |card| %>
      <tr>
        <td><%= card.original_text %></td>
        <td><%= card.translated_text %></td>
        <td><%= card.review_date.strftime("%d/%m/%Y") %></td>
        <td><%= link_to "редактировать", edit_card_path(card),
                                         class: "btn btn-default" %></td>
        <td><%= link_to "удалить", card, method: :delete,
                                         data: { confirm: "Удалить?" },
                                         class: "btn btn-default" %></td>
      </tr>
    <% end %>
  </tbody>
</table>